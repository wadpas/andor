import { Text, View } from 'react-native'

export default function Product() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>Shop</Text>
    </View>
  )
}
